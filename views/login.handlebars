<html>

<head>
  <title></title>
 <link href="/css/login.css" rel="stylesheet">
</head>

<body>
  <div id="logo"> 
 <h1><i>Nelisa's Spaza Shop</i></h1>
</div> 

 <section class="Nelisa-login">
  
  <form form method="post" action="/home">  
    <div id="fade-box">
      <input type="text" name="username" value="" placeholder="Username or Email" required>
      <input type="password" name="password" value="" placeholder="Password" required>	
        <p class="submit"><input type="submit" name="commit" value="Login"></p>
   <a href="signup"><strong>Dont Have Account??</strong>Sign Up  </a> 
   </div>     
</form>
 </section> 
 <!--script type="text/javascript">
 var count = 2;
 function validate() {
 	var un = document.myform.username.value;
 	var pw = document.myform.password.value;
 	var valid = false;

var unArray = ["Admin", "Sbuja", "Linkie"];  // as many as you like - no comma after final entry
var pwArray = ["Coder123", "Password2"];  // the corresponding passwords;

for (var i=0; i <unArray.length; i++) {
	if ((un == unArray[i]) && (pw == pwArray[i])) {
		valid = true;
		break;
	}
}

if (valid) {
	alert ("Login was successful");
	location = "http://localhost:3000";
	return false;
}

var t = " tries";
if (count == 1) {t = " try"}

	if (count >= 1) {
		alert ("Invalid username and/or password.  You have " + count + t + " left.");
		document.myform.username.value = "";
		document.myform.password.value = "";
		setTimeout("document.myform.username.focus()", 25);
		setTimeout("document.myform.username.select()", 25);
		count --;
	}

	else {
		alert ("Still incorrect! You have no more tries left!");
		document.myform.username.value = "No more tries allowed!";
		document.myform.password.value = "";
		document.myform.username.disabled = true;
		document.myform.password.disabled = true;
		return false;
	}

}
 </script-->
</body>
</html>